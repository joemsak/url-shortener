<% content_for :title, t('views.urls.new.title') %>

<div class="row">
  <h1 class='page-title'><%= t('views.urls.new.page_title') %></h1>
</div>

<div id="shortened_url" class="row">
  <%= render 'form' %>
</div>
