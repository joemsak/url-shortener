<% flash.each do |type, msg| %>
  <div class="row">
    <div class="small-12">
      <div class='alert-box <%= type %> radius'>
        <%= msg %>
        <a href='#' class='close'>&times;</a>
      </div>
    </div>
  </div>
<% end %>
